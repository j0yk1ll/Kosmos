# Kosmos Pre-commit Configuration
# Install: pip install pre-commit && pre-commit install

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        exclude: '^kosmos-claude-scientific-skills/'
      - id: end-of-file-fixer
        exclude: '^kosmos-claude-scientific-skills/'
      - id: check-yaml
        args: ['--allow-multiple-documents']
        exclude: '^kosmos-claude-scientific-skills/'
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: debug-statements
        exclude: '^kosmos-claude-scientific-skills/'
      - id: check-toml
        exclude: '^kosmos-claude-scientific-skills/'

  - repo: https://github.com/psf/black
    rev: 24.3.0
    hooks:
      - id: black
        language_version: python3.11
        args: ['--line-length=100']
        exclude: '^kosmos-claude-scientific-skills/'

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.14.7
    hooks:
      - id: ruff
        args: ['--fix', '--unsafe-fixes']
        exclude: '^kosmos-claude-scientific-skills/'

  # - repo: local
  #   hooks:
  #     - id: verify-production
  #       name: run production verification
  #       # Use the script directly so pre-commit runs it via its shebang
  #       # (entry was previously `bash scripts/verify_production.sh` which
  #       # caused pre-commit to look for `bash` inside the repo root).
  #       entry: scripts/verify_production.sh
  #       language: script
  #       pass_filenames: false
  #       stages: [pre-push]
  #       always_run: true
